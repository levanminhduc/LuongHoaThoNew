# AGENTS.md

Hướng dẫn cho AI agent làm việc trong repo này.

**Toàn bộ nội dung nằm ở [`CLAUDE.md`](CLAUDE.md)** — định vị dự án, non-negotiables, lệnh đã verify, và các pitfall repo-specific (build phải dùng webpack, middleware nằm ở `proxy.ts`, `npm test` fail dưới pnpm, `van_phong` bypass department filter...).

Đọc `CLAUDE.md` trước khi sửa bất cứ thứ gì. Đừng chép nội dung sang đây — một nguồn sự thật duy nhất.
